name: python-3.13
on:
  push:
    branches:
      - master
  pull_request:
    types:
      - opened

jobs:
  ubuntu-latest:
    uses: ./.github/workflows/main.yml
    with:
      python-version: "3.13"
